<!DOCTYPE html>
<html>
<head>
    <title>Space Trap</title>
    <meta charset="utf-8" />
    <style>
        include Leaderboard.css
    </style>
</head>
<body>
    <div>
        <div class="caption">Space Trap Leaderboard</div>
        <table>
            <tr>
                <th>Score</th>
                <th>Player</th>
            </tr>
            each item in scoreArr
                if item._id.toString() === id
                    <tr class="player">
                        <td>#{item.score}</td>
                        <td>#{item.name}</td>
                    </tr>
                else
                    <tr>
                        <td>#{item.score}</td>
                        <td>#{item.name}</td>
                    </tr>
        </table>
    </div>
</body>
<html>